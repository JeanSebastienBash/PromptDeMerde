Genera il miglior prompt di contesto per un profilo chiamato #{{title}}.
Il tag deve essere esattamente "{{title}}" (maiuscola iniziale, senza spazi).
Le istruzioni devono descrivere con precisione il comportamento atteso durante la riformulazione dei prompt utente, coerente con questo titolo.
