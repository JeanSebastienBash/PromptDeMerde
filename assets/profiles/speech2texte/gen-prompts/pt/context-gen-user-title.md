Gera o melhor prompt de contexto para um perfil chamado #{{title}}.
A tag deve ser exactamente "{{title}}" (maiúscula inicial, sem espaços).
As instruções devem descrever com precisão o comportamento esperado ao reformular prompts de utilizador, coerente com este título.
